{
  "name": "api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "build": "bun build src/index.ts --outdir dist --target bun",
    "start": "bun run dist/index.js",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint src"
  },
  "dependencies": {
    "@magi-boilerplate/shared": "workspace:*",
    "@magi-boilerplate/utils": "workspace:*",
    "ai": "^6.0.116",
    "hono": "^4.0.0",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@magi-boilerplate/config-eslint": "workspace:*",
    "@types/bun": "1.3.10",
    "typescript": "^5.9.0",
    "vitest": "^4.0.0"
  }
}
